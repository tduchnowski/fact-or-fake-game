NEXT:
  - refactor bot code
  - test bot code
  - fix the postgres password in Kubernetes, don't hardcode it in connection string 
  - test the middleware that logs activity time
  - more readable error messages for users
  - room size cap
  - in PlayersInfo use flag IsActive and then remove rooms based on the active users count
  - get rid of all the warnings
  - more useful readiness healthcheck
  - ensure that postgres and redis are ready before starting server, and server must start before bot

FURTHER IMPROVEMENTS:
  - integration tests
  - end to end tests (client simulations)
  - maximum performance logging
